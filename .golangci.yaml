linters-settings:
  errcheck:
    check-type-assertions: true
  goconst:
    min-len: 2
    min-occurrences: 3

linters:
  disable-all: true
  enable:
    - errcheck
    - goconst
    - govet
    - staticcheck
    - gofmt

run:
  issues-exit-code: 1